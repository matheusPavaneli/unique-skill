---
description: Audit a frontend surface against real budgets before calling it done — Core Web Vitals, bundle, WCAG 2.2 AA, state matrix, 320px and 200% zoom, promised fallbacks.
argument-hint: "[surface, path, or blank for the whole project]"
---

Use the `ship-audit` skill for: $ARGUMENTS

Measure. A number that was not measured is reported as "not measured", never estimated as
fact. Check every fallback `.unique/stack.md` promised is actually exercised.

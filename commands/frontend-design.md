---
description: Commit a surface to a binding design contract — originality level, mode, budget, tokens — and build it at senior product-engineering quality.
argument-hint: "[surface]"
---

Use the `frontend-design` skill for: $ARGUMENTS

Set ORIGINALITY (`native` | `benchmark(X)` | `signature`), MODE and BUDGET explicitly before
any visual decision. Write the contract to `.unique/contract.md`. For `signature`, the
falsifiable gates are required, not optional.

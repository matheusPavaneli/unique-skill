---
description: Implement a surface against the design contract and stack record — semantics and states first, heavy capabilities behind a lazy boundary.
argument-hint: "[surface or component]"
---

Use the `build-surface` skill for: $ARGUMENTS

Read `.unique/contract.md` and `.unique/stack.md` first and build against them. If either is
missing, say which and enter the step that produces it before writing code.

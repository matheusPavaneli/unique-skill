---
description: Turn a product idea into a written brief — subject, audience, surfaces, and the capability list stack-route consumes.
argument-hint: "[product idea]"
---

Use the `product-brief` skill for: $ARGUMENTS

Write the result to `.unique/brief.md`. When the brief names a capability that needs a
technical decision, say so and hand off to `stack-route`.
